# FSU 优化待办

> 从 `d5256f6` (2026-07-20) revert 出来的三个优化点，后续实现时逐个验证语法。

## ✅ 3. 提交评分缓存 — 已完成 (2026-07-21)

## ❌ 2. 候选排序优先级调整 — 已取消（没必要调）

---

## ✅ 1. 化学条件分流 — 已完成 (2026-07-21)

> 安全实现：不分数据源，都走 `_templateCache`，只是无化学要求时找到第一个可用球员就停，不做化学排序遍历。

**目标：** 有化学要求的 SBC 用缓存在内存过滤（反复试化学），无化学要求的用原生查询（快）。

**文件位置：** `【FSU】EAFC FUT WEB 增强器-26.09.yyh.js`

### 改动点

**① 填充前判断化学要求**（约 line 8654 附近）

```js
// 在 getItemByStep 中，构建 _templateCache 之后
const _hasChemReq = e.challenge && e.challenge.eligibilityRequirements &&
    e.challenge.eligibilityRequirements.some(req =>
        req.getFirstKey() === SBCEligibilityKey.CHEMISTRY_POINTS ||
        req.getFirstKey() === SBCEligibilityKey.ALL_PLAYERS_CHEMISTRY_POINTS
    );
```

**② 候选池分流**（约 line 8805 附近）

- `_hasChemReq = true` → 走 `_templateCache` 过滤 + `_.orderBy`（Set 优化 ExcludeDefIds / lock 过滤）
- `_hasChemReq = false` → 走 `events.getItemBy(2, rsCriteria)` 原生查询 + `_.orderBy`

**rsCriteria 参数：**
```js
{
    "NEdatabaseId": ExcludeDefIds,
    "LTrating": searchMaxRating,
    "bepos": indexPos,
    "lock": false
}
```

**注意：** `events.getItemBy(2, rsCriteria)` 的 `bepos` 只支持单 position 字符串，不支持多位置；而 `_templateCache` 用 `basePossiblePositions.includes(indexPos)` 兼容多位置球员。实现时注意确保无化学要求分支也能匹配到多位置球员。

---

## □ 2. 候选排序优先级调整

**目标：** 优先选低评分球员，节省高评分库存。

**位置：** 同一 `getItemByStep` 函数中，`satisfyPlayers` 排序处（约 line 8870 附近）

### 排序优先级

```
改动前（降序排列字段）:  阵容化学 → 球员化学 → 评分（升序）
改动后（排序字段顺序）:  评分（升序）→ 阵容化学（降序）→ 球员化学（降序）
```

实现代码：
```js
const firstCandidate = _.first(_.orderBy(
    satisfyPlayers,
    [
        item => item.player.rating,
        item => item.squadChemistry,
        item => item.playerChemistry
    ],
    ['asc', 'desc', 'desc']
));
```

---

## □ 3. 提交评分缓存

**目标：** 记住上次提交的评分列表，下次打开同一 SBC 时自动填入，减少手动输入。

### 改动位置 & 改动点

**① submitChallenge 拦截器**（约 line 12189 附近）

截获阵容评分：
```js
const originalSubmitChallenge = UTSBCService.prototype.submitChallenge;
UTSBCService.prototype.submitChallenge = function(o, a, i, n) {
    // 提交前捕获 11 人评分
    let _cachedRatings = null, _cachedChallengeId = null;
    try {
        if (o && o.squad && o.id) {
            const players = o.squad.getFieldPlayers();
            const ratings = players
                .filter(p => p._item && typeof p._item.rating === 'number')
                .map(p => p._item.rating)
                .sort((a, b) => b - a);
            if (ratings.length === 11) {
                _cachedRatings = ratings.join(",");
                _cachedChallengeId = o.id;
            }
        }
    } catch(e) {}
    // ... 原有逻辑 ...
```

提交成功后存入缓存：
```js
// 在成功回调中
if (_cachedRatings && _cachedChallengeId && t.data && t.data.setId) {
    const key = `${t.data.setId}#${_cachedChallengeId}`;
    let cache = JSON.parse(GM_getValue("SBCLastRatings", "{}"));
    cache[key] = _cachedRatings;
    GM_setValue("SBCLastRatings", JSON.stringify(cache));
}
```

**② 补全按钮默认值**（约 line 14986 附近）

读取缓存填入 placeholder：
```js
// 优先使用上次提交的评分缓存
try {
    const setId = thisController._set?.id;
    const challengeId = thisController._challenge?.id;
    if (setId && challengeId) {
        const allCache = JSON.parse(GM_getValue("SBCLastRatings", "{}"));
        const lastStr = allCache[`${setId}#${challengeId}`];
        if (lastStr) inputText = [fy("squadcmpl.placeholder"), lastStr];
    }
} catch(e) {}

// 缓存没有命中时走原有逻辑
if (!inputText) {
    // ... 原有的 fillRating 计算逻辑 ...
}
```

### 注意事项

- 缓存 key 为 `{setId}#{challengeId}`，注意 `setId` 和 `challengeId` 的正确获取
- 需要清理过期缓存（目前不清理，会无限增长）
- 缓存不跨 SBC 类型共享，每个 SBC 独立存储
